// Engine thread loop. To be filled in during MVP Phase 1.
//
// Responsibility: own the decoder + DSP + ring producer; consume Commands;
// drive decoded frames through the DSP into the ring buffer.
